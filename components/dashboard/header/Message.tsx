import MenuButton from "@/components/common/MenuButton";
import { IconMessage } from "@tabler/icons-react";

export default function Message() {
  return <MenuButton icon={IconMessage}>Message</MenuButton>;
}
